[project]
name = "client-app"
version = "0.1.0"
description = "create client app with fastapi "
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.115.12",
    "python-dotenv==1.2.2",
    "temporalio==1.29.0",
    "uvicorn==0.34.3",
]
